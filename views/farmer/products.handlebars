{{>navbar}}
<div class="container">
  {{#if success}}
  <div class="alert alert-success col-6 mx-auto position-fixed h-10 opacity-5 text-center z-index-n1 w-100">
    {{success}}
  </div>
  {{/if}}
  {{#if error}}
  <div class="alert alert-danger col-6 mx-auto position-fixed h-10 opacity-5 text-center z-index-n1 w-100">
    {{error}}
  </div>
  {{/if}}
</div>
<section class="bg-light py-4 my-5">
    <div class="container">
        <div class="row">
          {{!-- Contents should be here --}}
            {{!-- <div class="col-12">
                <h2 class="mb-3 text-success">Welcome To Farmers Connect</h2>
                <p class="lead">Buy fresh foods straight from the farm</p>
            </div> --}}
            {{#each products}}
            <div class="col-md-6 col-lg-4">
                <div class="card my-3">
                    <div class="card-thumbnail">
                        <a href="/farmer/product/{{id}}">
                        <img src={{imageUrl}} class="img-fluid image" alt="thumbnail"/>
                        </a>
                    </div>
                    <div class="card-body">
                        <h3 class="card-title"><a href="#" class="text-secondary">₦ {{price}}</a></h3>
                        <p class="card-text">{{description}}</p>
                        {{!-- <form action="/cart/add/{{id}}/" method="POST" id="add-to-cart">
                            <button class="btn btn-success" type="submit">Add To Cart</button>
                        </form> --}}
                        <button>Delete</button>
                        <button>Edit</button>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</section>